name: Release on Version Tag

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  PYTHONUNBUFFERED: 1

jobs:
  build-package:
    name: Build Python package
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.14"
          enable-cache: true

      - name: Sync dependencies
        run: uv sync

      - name: Build wheel and sdist
        run: uv build

      - name: Upload Python package artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-dist
          path: dist/*
          if-no-files-found: error

  build-binaries:
    name: Build executable (${{ matrix.target_name }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target_name: linux-x64
            executable_name: qa
            output_path: dist/qa
            zip_name: qa-linux-x64.zip
          - os: macos-latest
            target_name: macos-arm64
            executable_name: qa
            output_path: dist/qa
            zip_name: qa-macos-arm64.zip
          - os: windows-latest
            target_name: windows-x64
            executable_name: qa.exe
            output_path: dist/qa.exe
            zip_name: qa-windows-x64.zip

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.14"
          enable-cache: true

      - name: Sync dependencies
        run: uv sync

      - name: Build single-file executable
        run: uv run --with pyinstaller pyinstaller --onefile --name qa query_analyzer/__main__.py

      - name: Package executable (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          cp "${{ matrix.output_path }}" "${{ matrix.executable_name }}"
          chmod +x "${{ matrix.executable_name }}"
          zip "${{ matrix.zip_name }}" "${{ matrix.executable_name }}"

      - name: Package executable (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Copy-Item "${{ matrix.output_path }}" "${{ matrix.executable_name }}"
          Compress-Archive -Path "${{ matrix.executable_name }}" -DestinationPath "${{ matrix.zip_name }}"

      - name: Upload executable artifact
        uses: actions/upload-artifact@v4
        with:
          name: executable-${{ matrix.target_name }}
          path: ${{ matrix.zip_name }}
          if-no-files-found: error

  release:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs:
      - build-package
      - build-binaries

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets

      - name: Prepare release assets folder
        run: |
          mkdir -p upload
          find release-assets -type f -name "*.zip" -exec cp {} upload/ \;

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: upload/*.zip
          generate_release_notes: true
          draft: false
          prerelease: false
